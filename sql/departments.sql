CREATE TABLE IF NOT EXISTS `departments` (
`department_id`        int(11)       NOT NULL   AUTO_INCREMENT  COMMENT 'Position key', 
`name`                 varchar(100)  NOT NULL                	COMMENT 'The name of the Experience',
PRIMARY KEY (`department_id`)
);